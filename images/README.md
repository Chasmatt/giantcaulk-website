# images

Site references the following files. Drop them here (pre-sized per the main
README's resize snippet):

- `hero-sealing-1000.jpg` — home page hero, small
- `hero-sealing-1600.jpg` — home page hero, medium (default `src`)
- `hero-sealing-2400.jpg` — home page hero, large
- `hero-desk.jpg` — contact/thanks page background photo (desktop)
- `hero-desk-mobile.jpg` — same, smaller file for phones

Without these, the site still runs, but the hero shows a broken image and the
contact/thanks pages fall back to the flat paper-white background.
